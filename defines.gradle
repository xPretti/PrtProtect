ext {
    DEF_VERSION = '1.0.0'
    DEF_FOLDER_VERSION = "1.8.8"
    DEF_GROUP = 'dev.pretti.prtprotect'
    DEF_NAME = rootProject.name
    DEF_MAIN_CLASS = rootProject.group + "." + rootProject.name
    DEF_FINAL_NAME = "${rootProject.name}-${DEF_VERSION}.jar"

    DEF_FINAL_DIR = rootProject.findProperty('DEF_FINAL_DIR')
    DEF_REPOSITORY_DIR = rootProject.findProperty('DEF_REPOSITORY_DIR')
}
